# Hikvision Monitor Library - Requirements
# =========================================
# Install: pip install -r requirements.txt

# Core (no dependencies for basic usage)
# The library uses only Python standard library for core functionality

# Optional: Database support (SQLAlchemy models)
sqlalchemy>=2.0.0

# Optional: Progress bar for mass_health_check.py
tqdm>=4.60.0

# Optional: Async database support
# aiosqlite>=0.19.0  # For async SQLite
# asyncpg>=0.28.0    # For async PostgreSQL

# Development dependencies (uncomment if needed)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.0.0
